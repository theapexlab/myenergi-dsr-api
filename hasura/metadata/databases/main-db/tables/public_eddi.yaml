table:
  schema: public
  name: eddi
object_relationships:
  - name: aggregation_datum
    using:
      foreign_key_constraint_on: aggregationdataid
  - name: hub
    using:
      foreign_key_constraint_on: hubserialno
remote_relationships:
  - definition:
      to_source:
        field_mapping:
          serialno: serialno
        relationship_type: object
        source: dsr-db
        table:
          name: control_group_device
          schema: public
    name: eddi_control_group_device
