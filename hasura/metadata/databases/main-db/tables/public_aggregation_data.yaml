table:
  schema: public
  name: aggregation_data
object_relationships:
  - name: eddi
    using:
      foreign_key_constraint_on:
        column: aggregationdataid
        table:
          schema: public
          name: eddi
  - name: zappi
    using:
      foreign_key_constraint_on:
        column: aggregationdataid
        table:
          schema: public
          name: zappi
